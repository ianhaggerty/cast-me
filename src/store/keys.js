/**
 * These are keys used in the configuration store.
 */

export const SOURCE_ROOT = "source.root";
export const SOURCE_GLOB = "source.glob";
export const SOURCE_MOVE = "source.move";

export const OUTPUT_ROOT = "output.root";
